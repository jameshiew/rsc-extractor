When adding a new dependency:

- check you're adding the most up-to-date compatible version

When you finish:

- `cargo clippy` and fix any warnings
- `cargo +nightly fmt`
- there is no need to create summary documents, unless asked to
